{
  "name": "stage-b-the-verge",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "description": "Stage B prototype — the smallest loop that can kill you.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json --watch",
    "serve": "node serve.mjs",
    "start": "npm run build && node serve.mjs"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
