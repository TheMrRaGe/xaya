{
  "name": "stage-b-the-verge",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "description": "Stage B prototype — the smallest loop that can kill you.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json --watch",
    "serve": "node server.mjs",
    "start": "npm run build && node server.mjs"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  },
  "dependencies": {
    "ws": "^8.21.3"
  }
}
